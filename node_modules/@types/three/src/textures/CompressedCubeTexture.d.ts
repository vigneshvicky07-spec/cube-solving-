import { CompressedPixelFormat, TextureDataType } from "three/src/constants.js";
import { CompressedTexture, CompressedTextureImageData } from "three/src/textures/CompressedTexture.js";

export class CompressedCubeTexture extends CompressedTexture<CompressedTextureImageData[]> {
    readonly isCompressedCubeTexture: true;
    readonly isCubeTexture: true;

    constructor(
        images: CompressedTextureImageData[],
        format?: CompressedPixelFormat,
        type?: TextureDataType,
    );
}
