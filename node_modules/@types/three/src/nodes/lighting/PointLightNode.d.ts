import { PointLight } from "three/src/lights/PointLight.js";
import Node from "../core/Node.js";
import AnalyticLightNode from "three/src/nodes/lighting/AnalyticLightNode.js";
import { DirectLightData } from "three/src/nodes/lighting/LightsNode.js";
import PointShadowNode from "three/src/nodes/lighting/PointShadowNode.js";

export const directPointLight: (params: {
    color: Node;
    lightViewPosition: Node;
    cutoffDistance: Node;
    decayExponent: Node;
}) => DirectLightData;

declare class PointLightNode extends AnalyticLightNode<PointLight> {
    cutoffDistanceNode: Node;
    decayExponentNode: Node;

    constructor(light?: PointLight | null);

    setupShadowNode(): PointShadowNode;
}

export default PointLightNode;
